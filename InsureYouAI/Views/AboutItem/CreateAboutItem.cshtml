@model AboutItem
@{
    ViewData["Title"] = "CreateAboutItem";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">
    <div class="card-body">
        <div class="d-flex align-items-center">
            <h5 class="mb-0">Yeni Hakkımızda Detay Yazısı Girişi</h5>
        </div>
        <form method="post">
            <div class="form-group">
                <label>Detay</label>
                <input type="text" class="form-control" asp-for="Detail" placeholder="Detayı Giriniz" />
            </div>
            <button type="submit" class="btn btn-outline-primary mt-3">Kaydet</button>
        </form>
    </div>
</div>

